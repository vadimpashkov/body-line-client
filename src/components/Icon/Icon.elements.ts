import styled from 'styled-components';

export const IconSvg = styled.svg`
  --size: 12px;

  display: block;
  width: var(--size);
  height: var(--size);
  object-fit: cover;
  object-position: center;
  fill: currentColor;
`;
